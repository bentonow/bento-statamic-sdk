{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build && npm run copy-assets",
        "copy-assets": "mkdir -p ../../../public/vendor/bento-statamic/build && cp -r public/vendor/bento-statamic/build/* ../../../public/vendor/bento-statamic/build/",
        "prebuild": "mkdir -p public/vendor/bento-statamic/build"
    },
    "devDependencies": {
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
